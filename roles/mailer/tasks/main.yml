#- name: return hostname
#  command: /bin/hostname
#  register: prov_hostname
#- set_fact: hostname={{item.stdout}}
#  with_items: {{ prov_hostname }}
#- debug: var=prov_hostname
- mail:
    host='localhost'
    port=25
    to="Chris Grigor <chris@icemobile.com>"
    subject='Ansible-report'
    body='System {{ hostvars[inventory_hostname] }} has been successfully provisioned.'
